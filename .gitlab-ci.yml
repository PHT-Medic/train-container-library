run tests:
  stage: test
  image: python:3
  script:
    - pip install pytest pytest-cov -r requirements.txt
    - pip install .
    - pytest --cov=train_lib/
    - coverage xml
  artifacts:
    reports:
      cobertura: coverage.xml
